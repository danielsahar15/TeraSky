apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: hello-release-2
  namespace: default
spec:
  interval: 5m
  chart:
    spec:
      chart: hello
      version: '0.2.0'
      sourceRef:
        kind: HelmRepository
        name: chartmuseum
        namespace: flux-system
      interval: 1m
  values:
    image:
      repository:  terasky-repo
      tag: latest
